apiVersion: apps/v1

kind: Deployment

metadata:
  name: user-deployment

spec:
  replicas: 1

  selector:
    matchLabels:
      app: user-service

  template:

    metadata:
      labels:
        app: user-service

    spec:
      containers:
        - name: user-service

          image: docker.io/vinayksm86/learning-recommender-user-service:latest

          imagePullPolicy: Always

          ports:
            - containerPort: 8002
