apiVersion: v1
kind: Service

metadata:
  name: recommendation-service

spec:
  selector:
    app: recommendation-service

  type: NodePort

  ports:
    - name: http
      protocol: TCP
      port: 8001
      targetPort: 8001
      nodePort: 32620